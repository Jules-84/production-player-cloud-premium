# Production Player Cloud v3

Upload these files to the existing GitHub repository, replacing the old cloud files.
Render should redeploy automatically.

Cloud v3 adds live:
- current cue
- next cue
- elapsed time
- cue duration
- progress bar
- playing or paused state

Keep the existing DESKTOP_SECRET in Render unchanged.
